{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "title": "名称",
      "placeholder": "请输入",
      "required": "{{ $deps.requiredAll }}"
    },
    "age": {
      "type": "number",
      "title": "年龄",
      "required": "{{ $deps.requiredAll }}"
    }
  }
}
