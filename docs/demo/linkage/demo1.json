{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "title": "名称",
      "placeholder": "请输入",
      "required": "{{ $values.required }}"
    },
    "required": {
      "type": "boolean",
      "title": "是否必填"
    }
  }
}
